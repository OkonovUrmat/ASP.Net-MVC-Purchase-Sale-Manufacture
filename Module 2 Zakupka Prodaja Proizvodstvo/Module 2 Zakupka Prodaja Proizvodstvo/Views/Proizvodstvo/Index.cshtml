@model Module_2_Zakupka_Prodaja_Proizvodstvo.Models.ProizvodstvoListModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>



<form method="get">
    <div class="form-inline">
        @*<label class="control-label">Продукт: </label>
        @Html.DropDownList("product", Model.Productsi as SelectList,
                    htmlAttributes: new { @class = "form-control" })*@

        <label class="control-label">Сотрудник: </label>
        @Html.DropDownList("sotrudnik", Model.Sotrudniksi as SelectList,
                htmlAttributes: new { @class = "form-control" })

        <input type="submit" value="Фильтр" class="btn btn-default" />
    </div>
</form>
<table class="table">
    <tr>
        <th>ID</th>
        <th>Название продукта</th>
        <th>Сотрудник</th>
        <th>Количество</th>
        <th></th>
    </tr>

    @foreach (var item in Model.Proizvodstvosi)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.proizvodstvo_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gotov_product.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sotrudnik1.FIO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kol_vo)
            </td>
            <td></td>
            <td>@Html.ActionLink("Delete", "Delete", new { id = item.proizvodstvo_ID })</td>
            <td>@Html.ActionLink("Edit", "Edit", new { id = item.proizvodstvo_ID })</td>
        </tr>
    }

</table>
