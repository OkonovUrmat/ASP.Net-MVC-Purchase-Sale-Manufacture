@model Module_2_Zakupka_Prodaja_Proizvodstvo.Models.SyrieVM

@{
    ViewBag.Title = "Syrie";
}

<h2>Syrie</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<form method="get">
    <div class="form-inline">
        <label class="control-label">Название Сырья: </label>
        @Html.DropDownList("syrye", Model.Syryes as SelectList,
                    htmlAttributes: new { @class = "form-control" })
        <input type="submit" value="Фильтр" class="btn btn-default" />
    </div>
</form>*@
<table class="table">
    <tr>
        <th>ID</th>
        <th>Название сырья</th>
        <th>Единица измерения</th>
        <th>Сумма</th>
        <th>Количество</th>
        <th></th>
    </tr>


    @foreach (Module_2_Zakupka_Prodaja_Proizvodstvo.Models.Syrie item in ViewBag.item)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.syrie_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Edinic_izmer.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.summa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kol_vo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.syrie_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.syrie_id  })
            </td>
        </tr>
    }
</table>
